# Ignore all untracked files and directories in config/ and local/
# Tracked files (and future files added with `git add -f`) remain tracked by Git.
/config/*
/local/*

# OS/Editor temporary files
*.swp
*.tmp
.DS_Store
